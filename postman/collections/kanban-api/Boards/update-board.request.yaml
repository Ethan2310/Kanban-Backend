$kind: http-request
name: Update Board
method: PATCH
url: '{{baseUrl}}/api/boards/{boardId}'
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{token}}'
body:
  type: json
  content: |-
    {
      "name": "Example Name",
      "description": "Example description"
    }
